/**
 * Deprecated: E2EE Key Initialization is now managed centrally inside E2EEBackupProvider
 * to ensure proper coordination with the PIN-based Key Backup and Recovery system.
 */
export const useE2EEInit = () => {
  // Delegated to E2EEBackupProvider
};
